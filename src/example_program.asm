# Загрузка констант в регистры
R2 = 100
R3 = 200
R4 = 300

# Чтение из памяти (по адресу в R3) в R5
R5 = [R3]

# Запись значения из R4 в память по адресу (R2 + 50)
[R2 + 50] = R4

# Циклический сдвиг: R6 = R1 сдвинуть на R2
R6 = R1 >> R2

# Еще одна запись в память
[R5 + 100] = R6