# Этот файл должен вызывать ошибки
R0 = 16384      # Ошибка: константа > 16383
R8 = 100        # Ошибка: регистр 8 не существует
[R0 + 65536] = R0  # Ошибка: смещение > 65535
R9 = R1 >> R2   # Ошибка: регистр 9 не существует